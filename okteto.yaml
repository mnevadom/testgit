deploy:
  remote: true
  commands:
  - name: Prepare Env
    command: |
      echo $GIT_SSH_PEM | base64 -d > /tmp/ssh.pem
      chmod 400 /tmp/ssh.pem
      git -c core.sshCommand="ssh -i /tmp/ssh.pem -F /dev/null" clone git@github.com:mnevadom/k8s-docs.git
